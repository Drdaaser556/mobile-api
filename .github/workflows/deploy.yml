name: Deploy to InfinityFree

on:
  push:
    branches:
      - main

jobs:
  ftp-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Upload via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.ftpupload.net }}
          password: ${{ secrets.hellovictor420 }}
          username: ${{ secrets.if0_39501146 }}    
          server-dir: /htdocs/
